:80 {
    encode gzip

    handle_path /api/* {
        reverse_proxy server:3000
    }

    handle /api {
        reverse_proxy server:3000
    }

    handle {
        reverse_proxy web:3001
    }
}
