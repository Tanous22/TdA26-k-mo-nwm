:80 {
    encode gzip

    handle_path /api/* {
        reverse_proxy server:3000
    }

    handle /api {
        reverse_proxy server:3000
    }

    # Pokud web ještě není nahozený, vracej 502 – to je OK a TdA počká
    handle {
        reverse_proxy web:4173
    }
}
